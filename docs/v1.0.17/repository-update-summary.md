# Repository update summary

The v1.0.17 repository update adds documentation and source notes for the metadata-editing release.

Merged content should include:

- `docs/v1.0.17/` release documentation;
- `source/v1.0.17/` source-change notes and selected code snippets;
- `update-v1.0.17.example.json` reviewed update-feed example.

The signed APK itself should be uploaded separately as a GitHub Release asset before the live `update.json` feed is changed.
