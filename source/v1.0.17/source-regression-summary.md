# v1.0.17 source regression summary

The source-regression suite was updated for version 1.0.17 and passed.

New checks cover:

- app identity `1.0.17 / 10017`;
- `Edit metadata` menu wiring;
- metadata read/write Termux command types;
- FFprobe metadata read command;
- FFmpeg metadata write command;
- common genre chooser entries;
- year/date validation;
- Android media scanner refresh after metadata save;
- retention of v1.0.16, v1.0.15 and v1.0.14 fixes.
