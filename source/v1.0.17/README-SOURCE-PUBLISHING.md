# Source publishing note

The ChatGPT build workspace generated the complete public source archive:

```text
ITthuteFetchAudioVid-v1.0.17-source.zip
```

For repository review, this folder commits focused source notes and representative snippets rather than private build outputs. The generated source archive is provided separately to the app owner and excludes private signing material.

The public files here are sufficient to review the v1.0.17 code-level changes:

- metadata menu entry;
- Termux command types;
- FFprobe metadata read flow;
- FFmpeg metadata write flow;
- genre picker and field matrix;
- regression-test coverage.
